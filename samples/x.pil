{
    glob a b c d;
    
    print_str("A B C: ");
    a = read_int();
    b = read_int();
    c = read_int();

    d = -b + 4*a*c;
    if (d < 0) print_str("No solutions.\n");
    if (d == 0) {
        print_str("X = ");
        println_int(-b/(2*a));
    }
    else print_str("2 solutions. No sqrt function yet...\n");
    return;
}
